### Mobile (NativeScript) specific libs

This is intended for NativeScript specific custom libs/plugins only.

These are excluded from root tsc compilation and are only consumed via the NativeScript app.
