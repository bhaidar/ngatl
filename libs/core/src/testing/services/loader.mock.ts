export class MockPlatformLoaderService {
  public show(options?: any) {
    // can use following to debug tests:
    // console.log('show', options);
  }

  public hide(options?: any) {
    // can use following to debug tests:
    // console.log('hide', options);
  }
}
